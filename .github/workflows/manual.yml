name: manual-trigger

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Choose an environment"
        required: true
        default: "staging"

jobs:
  run-manual:
    runs-on: ubuntu-latest
    steps:
      - name: Print environment
        run: echo "Running"

